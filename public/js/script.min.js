function resizeVideo() {
    var vWidth, vHeight, wHeight = window.innerHeight, wWidth = window.innerWidth, wRatio = wWidth / wHeight;
    if (wRatio >= vRatio) vHeight = wHeight - 121, vWidth = vHeight * vRatio, v.width = vWidth, 
    v.height = vHeight, v.style.left = (wWidth - vWidth) / 2 + "px"; else {
        vWidth = wWidth - 40, vHeight = vWidth * (1 / vRatio);
        try {
            v.width = vWidth, v.height = vHeight, v.style.left = "20px";
        } catch (event) {}
    }
    v.style.position = "absolute", v.style.top = "100px", v.style.zIndex = 9999;
}

var v = document.getElementById("video"), vRatio = 960 / 641;

window.addEventListener ? window.addEventListener("resize", function() {
    resizeVideo();
}) : window.attachEvent("resize", function() {
    resizeVideo();
}), resizeVideo();